[package]
name = "rebon-node-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "Resolution, version gating, and verified managed installation of the Node runtime that hosts the rebon plugin plane."

[dependencies]
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true

[dev-dependencies]
# Not in the workspace dependency table; only the test modules need it.
tempfile = "3"
