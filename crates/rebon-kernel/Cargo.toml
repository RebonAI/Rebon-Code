[package]
name = "rebon-kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "Minimal plugin kernel: Cordis-isomorphic context tree, disposer scopes, dual-plane (typed + JSON) service registry, and typed event bus with emit/bail/waterfall dispatch. The seed that everything else in rebon migrates onto as plugins."

[dependencies]
serde_json.workspace = true
thiserror.workspace = true
tracing.workspace = true
